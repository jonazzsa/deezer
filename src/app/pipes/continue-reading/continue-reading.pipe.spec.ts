import { ContinueReadingPipe } from './continue-reading.pipe';

describe('ContinueReadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ContinueReadingPipe();
    expect(pipe).toBeTruthy();
  });
});
