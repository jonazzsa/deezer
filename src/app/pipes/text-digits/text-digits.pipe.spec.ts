import { TextDigitsPipe } from './text-digits.pipe';

describe('TextDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new TextDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
