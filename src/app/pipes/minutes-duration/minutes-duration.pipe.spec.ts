import { MinutesDurationPipe } from './minutes-duration.pipe';

describe('MinutesDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
